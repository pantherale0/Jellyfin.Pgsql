diff --git a/Emby.Server.Implementations/Plugins/PluginManager.cs b/Emby.Server.Implementations/Plugins/PluginManager.cs
index f699c99d85..c9fd9b4067 100644
--- a/Emby.Server.Implementations/Plugins/PluginManager.cs
+++ b/Emby.Server.Implementations/Plugins/PluginManager.cs
@@ -777,6 +777,13 @@ namespace Emby.Server.Implementations.Plugins
                         lastName = entry.Name;
                         continue;
                     }
+
+                    // Non-active plugins of a newly seen name must not fall through into
+                    // superseded-version cleanup (that path assumes lastName refers to
+                    // another version of *this* plugin). Covers Disabled, Malfunctioned,
+                    // NotSupported, etc. See: https://github.com/jellyfin/jellyfin/issues/15897
+                    // and https://github.com/jellyfin/jellyfin/pull/15900
+                    continue;
                 }
 
                 if (string.IsNullOrEmpty(lastName))
