diff --git a/src/apps/stable/features/search/api/useSearchItems.ts b/src/apps/stable/features/search/api/useSearchItems.ts
index 81e72ce47f..b94fbcdeff 100644
--- a/src/apps/stable/features/search/api/useSearchItems.ts
+++ b/src/apps/stable/features/search/api/useSearchItems.ts
@@ -5,6 +5,7 @@ import { useInfiniteQuery } from '@tanstack/react-query';
 import { CardShape } from 'components/cardbuilder/utils/shape';
 import { useApi } from '../../../../../hooks/useApi';
 import * as userSettings from 'scripts/settings/userSettings';
+import { getInfiniteScrollLimit } from 'scripts/infiniteScrollHelper';
 import { addSection, getCardOptionsFromType, getItemTypesFromCollectionType, getTitleFromType, isLivetv, isMovies, isMusic, isTVShows, sortSections } from '../utils/search';
 import { useArtistsSearch } from './useArtistsSearch';
 import { usePeopleSearch } from './usePeopleSearch';
@@ -17,19 +18,13 @@ import { useProgramsSearch } from './useProgramsSearch';
 import { LIVETV_CARD_OPTIONS } from '../constants/liveTvCardOptions';
 
 const DEFAULT_SEARCH_LIMIT = 800;
-const MIN_INFINITE_PAGE_SIZE = 40;
 
 const getSearchPageSize = (infiniteScroll: boolean) => {
     if (!infiniteScroll) {
         return DEFAULT_SEARCH_LIMIT;
     }
 
-    const libraryPageSize = userSettings.libraryPageSize();
-    if (libraryPageSize > 0 && libraryPageSize < MIN_INFINITE_PAGE_SIZE) {
-        return MIN_INFINITE_PAGE_SIZE;
-    }
-
-    return libraryPageSize || 100;
+    return getInfiniteScrollLimit(40) || 100;
 };
 
 const dedupeItems = (items: BaseItemDto[]) => {
diff --git a/src/components/toolbar/AppUserMenu.tsx b/src/components/toolbar/AppUserMenu.tsx
index 029c0e4bb0..e4cebc990a 100644
--- a/src/components/toolbar/AppUserMenu.tsx
+++ b/src/components/toolbar/AppUserMenu.tsx
@@ -5,6 +5,7 @@ import DashboardIcon from '@mui/icons-material/Dashboard';
 import Download from '@mui/icons-material/Download';
 import Edit from '@mui/icons-material/Edit';
 import Logout from '@mui/icons-material/Logout';
+import People from '@mui/icons-material/People';
 import PhonelinkLock from '@mui/icons-material/PhonelinkLock';
 import Settings from '@mui/icons-material/Settings';
 import Storage from '@mui/icons-material/Storage';
@@ -60,6 +61,11 @@ const AppUserMenu: FC<AppUserMenuProps> = ({
         onMenuClose();
     }, [ onMenuClose ]);
 
+    const onSwitchUserClick = useCallback(() => {
+        Dashboard.switchUser();
+        onMenuClose();
+    }, [ onMenuClose ]);
+
     const onSelectServerClick = useCallback(() => {
         Dashboard.selectServer();
         onMenuClose();
@@ -201,6 +207,17 @@ const AppUserMenu: FC<AppUserMenuProps> = ({
                 </MenuItem>
             )}
 
+            <MenuItem
+                onClick={onSwitchUserClick}
+            >
+                <ListItemIcon>
+                    <People />
+                </ListItemIcon>
+                <ListItemText>
+                    {globalize.translate('ButtonSwitchUser')}
+                </ListItemText>
+            </MenuItem>
+
             <MenuItem
                 onClick={onLogoutClick}
             >
diff --git a/src/controllers/movies/movies.js b/src/controllers/movies/movies.js
index f38469275d..42af1d3738 100644
--- a/src/controllers/movies/movies.js
+++ b/src/controllers/movies/movies.js
@@ -1,5 +1,6 @@
 import loading from '../../components/loading/loading';
 import * as userSettings from '../../scripts/settings/userSettings';
+import { bindInfiniteScroll, getInfiniteScrollLimit } from '../../scripts/infiniteScrollHelper';
 import libraryBrowser from '../../scripts/libraryBrowser';
 import { AlphaPicker } from '../../components/alphaPicker/alphaPicker';
 import listView from '../../components/listview/listview';
@@ -54,6 +55,7 @@ export default function (view, params, tabContent, options) {
 
     const afterRefresh = (result) => {
         setFilterStatus(tabContent, query);
+        let isAppend = false;
 
         function onNextPageClick() {
             if (isLoading) {
@@ -80,7 +82,7 @@ export default function (view, params, tabContent, options) {
         if (!userSettings.enableInfiniteScroll()) {
             window.scrollTo(0, 0);
         } else {
-            const isAppend = pendingAppend;
+            isAppend = pendingAppend;
             pendingAppend = false;
 
             // Keep scroll position only when appending the next page.
@@ -141,22 +143,17 @@ export default function (view, params, tabContent, options) {
 
         loading.hide();
 
-        import('../../components/autoFocuser').then(({ default: autoFocuser }) => {
-            autoFocuser.autoFocus(tabContent);
-        });
+        if (!isAppend) {
+            import('../../components/autoFocuser').then(({ default: autoFocuser }) => {
+                autoFocuser.autoFocus(tabContent);
+            });
+        }
     };
 
     const getItemsHtml = (items) => {
-        let html;
+        let html = '';
         const viewStyle = this.getCurrentViewStyle();
 
-        if (pendingAppend && userSettings.enableInfiniteScroll()) {
-            const itemsContainer = tabContent.querySelector('.itemsContainer');
-            html = itemsContainer.innerHTML;
-        } else {
-            html = '';
-        }
-
         if (viewStyle == 'Thumb') {
             html += cardBuilder.getCardsHtml({
                 items: items,
@@ -319,24 +316,18 @@ export default function (view, params, tabContent, options) {
         });
 
         if (userSettings.enableInfiniteScroll()) {
-            view.addEventListener('viewdestroy', () => {
-                scrollController.abort();
-            }, { signal: scrollController.signal });
-
-            window.addEventListener('scroll', () => {
-                const scrollTop = window.scrollY || window.pageYOffset;
-                const scrollHeight = document.documentElement.scrollHeight || document.body.scrollHeight;
-                const clientHeight = document.documentElement.clientHeight || window.innerHeight;
-                const scrollPercentage = (scrollTop / (scrollHeight - clientHeight)) * 100;
-
-                const isNearBottom = scrollPercentage >= 95;
-
-                // check if tabelement is active else dont run reloaditems
-                if (isNearBottom && !isLoading && hasMoreitems && tabElement.classList.contains('is-active')) {
+            bindInfiniteScroll({
+                view,
+                tabElement,
+                itemsContainer,
+                scrollController,
+                shouldLoadMore: () => !isLoading && hasMoreitems,
+                onLoadMore: () => {
                     pendingAppend = true;
+                    itemsContainer.appendOnRefresh = true;
                     itemsContainer.refreshItems();
                 }
-            }, { signal: scrollController.signal });
+            });
         }
 
         tabElement.querySelector('.btnPlayAll').addEventListener('click', playAll);
@@ -370,11 +361,7 @@ export default function (view, params, tabContent, options) {
 
     query = userSettings.loadQuerySettings(savedQueryKey, query);
 
-    if (userSettings.enableInfiniteScroll() && userSettings.libraryPageSize() < 40) {
-        query.Limit = 40;
-    } else {
-        query.Limit = userSettings.libraryPageSize();
-    }
+    query.Limit = getInfiniteScrollLimit(40);
 
     this.showFilterMenu = function () {
         import('../../components/filterdialog/filterdialog').then(({ default: FilterDialog }) => {
diff --git a/src/controllers/music/musicalbums.js b/src/controllers/music/musicalbums.js
index 25b457bb9b..b0c08633ed 100644
--- a/src/controllers/music/musicalbums.js
+++ b/src/controllers/music/musicalbums.js
@@ -6,6 +6,7 @@ import AlphaPicker from '../../components/alphaPicker/alphaPicker';
 import listView from '../../components/listview/listview';
 import cardBuilder from '../../components/cardbuilder/cardBuilder';
 import * as userSettings from '../../scripts/settings/userSettings';
+import { bindInfiniteScroll, getInfiniteScrollLimit } from '../../scripts/infiniteScrollHelper';
 import globalize from '../../lib/globalize';
 import Events from '../../utils/events.ts';
 import { setFilterStatus } from 'components/filterdialog/filterIndicator';
@@ -86,11 +87,7 @@ export default function (view, params, tabContent) {
         loading.show();
         isLoading = true;
         const query = getQuery();
-        if (userSettings.enableInfiniteScroll() && userSettings.libraryPageSize() < 50) {
-            query.Limit = 50;
-        } else {
-            query.Limit = userSettings.libraryPageSize();
-        }
+        query.Limit = getInfiniteScrollLimit(50);
         setFilterStatus(tabContent, query);
 
         ApiClient.getItems(ApiClient.getCurrentUserId(), query).then((result) => {
@@ -205,7 +202,7 @@ export default function (view, params, tabContent) {
 
             const itemsContainer = tabContent.querySelector('.itemsContainer');
             if (append) {
-                itemsContainer.innerHTML += html;
+                itemsContainer.insertAdjacentHTML('beforeend', html);
             } else {
                 itemsContainer.innerHTML = html;
             }
@@ -214,9 +211,11 @@ export default function (view, params, tabContent) {
             loading.hide();
             isLoading = false;
 
-            import('../../components/autoFocuser').then(({ default: autoFocuser }) => {
-                autoFocuser.autoFocus(tabContent);
-            });
+            if (!append) {
+                import('../../components/autoFocuser').then(({ default: autoFocuser }) => {
+                    autoFocuser.autoFocus(tabContent);
+                });
+            }
         });
     };
 
@@ -324,23 +323,16 @@ export default function (view, params, tabContent) {
         });
 
         if (userSettings.enableInfiniteScroll()) {
-            view.addEventListener('viewdestroy', () => {
-                scrollController.abort();
-            }, { signal: scrollController.signal });
-
-            window.addEventListener('scroll', () => {
-                const scrollTop = window.scrollY || window.pageYOffset;
-                const scrollHeight = document.documentElement.scrollHeight || document.body.scrollHeight;
-                const clientHeight = document.documentElement.clientHeight || window.innerHeight;
-                const scrollPercentage = (scrollTop / (scrollHeight - clientHeight)) * 100;
-
-                const isNearBottom = scrollPercentage >= 95;
-
-                // check if tabelement is active else dont run reloaditems
-                if (isNearBottom && !isLoading && hasMoreitems && tabElement.classList.contains('is-active')) {
+            bindInfiniteScroll({
+                view,
+                tabElement,
+                itemsContainer,
+                scrollController,
+                shouldLoadMore: () => !isLoading && hasMoreitems,
+                onLoadMore: () => {
                     reloadItems({ append: true });
                 }
-            }, { signal: scrollController.signal });
+            });
         }
 
         tabElement.querySelector('.btnPlayAll').addEventListener('click', playAll);
diff --git a/src/controllers/music/musicartists.js b/src/controllers/music/musicartists.js
index a3bfc340dd..1004bf4ae3 100644
--- a/src/controllers/music/musicartists.js
+++ b/src/controllers/music/musicartists.js
@@ -5,6 +5,7 @@ import { AlphaPicker } from '../../components/alphaPicker/alphaPicker';
 import listView from '../../components/listview/listview';
 import cardBuilder from '../../components/cardbuilder/cardBuilder';
 import * as userSettings from '../../scripts/settings/userSettings';
+import { bindInfiniteScroll, getInfiniteScrollLimit } from '../../scripts/infiniteScrollHelper';
 import Events from '../../utils/events.ts';
 import { setFilterStatus } from 'components/filterdialog/filterIndicator';
 
@@ -70,11 +71,7 @@ export default function (view, params, tabContent, options) {
         loading.show();
         isLoading = true;
         const query = getQuery();
-        if (userSettings.enableInfiniteScroll() && userSettings.libraryPageSize() < 50) {
-            query.Limit = 50;
-        } else {
-            query.Limit = userSettings.libraryPageSize();
-        }
+        query.Limit = getInfiniteScrollLimit(50);
         setFilterStatus(tabContent, query);
 
         const promise = options.mode == 'albumartists' ?
@@ -188,7 +185,7 @@ export default function (view, params, tabContent, options) {
 
             const itemsContainer = tabContent.querySelector('.itemsContainer');
             if (append) {
-                itemsContainer.innerHTML += html;
+                itemsContainer.insertAdjacentHTML('beforeend', html);
             } else {
                 itemsContainer.innerHTML = html;
             }
@@ -197,9 +194,11 @@ export default function (view, params, tabContent, options) {
             loading.hide();
             isLoading = false;
 
-            import('../../components/autoFocuser').then(({ default: autoFocuser }) => {
-                autoFocuser.autoFocus(tabContent);
-            });
+            if (!append) {
+                import('../../components/autoFocuser').then(({ default: autoFocuser }) => {
+                    autoFocuser.autoFocus(tabContent);
+                });
+            }
         });
     };
 
@@ -269,23 +268,16 @@ export default function (view, params, tabContent, options) {
             reloadItems();
         });
         if (userSettings.enableInfiniteScroll()) {
-            view.addEventListener('viewdestroy', () => {
-                scrollController.abort();
-            }, { signal: scrollController.signal });
-
-            window.addEventListener('scroll', () => {
-                const scrollTop = window.scrollY || window.pageYOffset;
-                const scrollHeight = document.documentElement.scrollHeight || document.body.scrollHeight;
-                const clientHeight = document.documentElement.clientHeight || window.innerHeight;
-                const scrollPercentage = (scrollTop / (scrollHeight - clientHeight)) * 100;
-
-                const isNearBottom = scrollPercentage >= 95;
-
-                // check if tabelement is active else dont run reloaditems
-                if (isNearBottom && !isLoading && hasMoreitems && tabElement.classList.contains('is-active')) {
+            bindInfiniteScroll({
+                view,
+                tabElement,
+                itemsContainer,
+                scrollController,
+                shouldLoadMore: () => !isLoading && hasMoreitems,
+                onLoadMore: () => {
                     reloadItems({ append: true });
                 }
-            }, { signal: scrollController.signal });
+            });
         }
     };
 
diff --git a/src/controllers/music/songs.js b/src/controllers/music/songs.js
index 4faf9d1bda..3a5f55c2c6 100644
--- a/src/controllers/music/songs.js
+++ b/src/controllers/music/songs.js
@@ -5,6 +5,7 @@ import listView from '../../components/listview/listview';
 import loading from '../../components/loading/loading';
 import { playbackManager } from '../../components/playback/playbackmanager';
 import * as userSettings from '../../scripts/settings/userSettings';
+import { bindInfiniteScroll, getInfiniteScrollLimit } from '../../scripts/infiniteScrollHelper';
 import globalize from '../../lib/globalize';
 import Dashboard from '../../utils/dashboard';
 import Events from '../../utils/events.ts';
@@ -56,11 +57,7 @@ export default function (view, params, tabContent) {
         loading.show();
         isLoading = true;
         const query = getQuery();
-        if (userSettings.enableInfiniteScroll() && userSettings.libraryPageSize() < 50) {
-            query.Limit = 50;
-        } else {
-            query.Limit = userSettings.libraryPageSize();
-        }
+        query.Limit = getInfiniteScrollLimit(50);
 
         setFilterStatus(tabContent, query);
 
@@ -149,7 +146,7 @@ export default function (view, params, tabContent) {
 
             const itemsContainer = tabContent.querySelector('.itemsContainer');
             if (append) {
-                itemsContainer.innerHTML += html;
+                itemsContainer.insertAdjacentHTML('beforeend', html);
             } else {
                 itemsContainer.innerHTML = html;
             }
@@ -161,9 +158,11 @@ export default function (view, params, tabContent) {
             loading.hide();
             isLoading = false;
 
-            import('../../components/autoFocuser').then(({ default: autoFocuser }) => {
-                autoFocuser.autoFocus(page);
-            });
+            if (!append) {
+                import('../../components/autoFocuser').then(({ default: autoFocuser }) => {
+                    autoFocuser.autoFocus(page);
+                });
+            }
         });
     }
 
@@ -244,23 +243,16 @@ export default function (view, params, tabContent) {
         });
 
         if (userSettings.enableInfiniteScroll()) {
-            view.addEventListener('viewdestroy', () => {
-                scrollController.abort();
-            }, { signal: scrollController.signal });
-
-            window.addEventListener('scroll', () => {
-                const scrollTop = window.scrollY || window.pageYOffset;
-                const scrollHeight = document.documentElement.scrollHeight || document.body.scrollHeight;
-                const clientHeight = document.documentElement.clientHeight || window.innerHeight;
-                const scrollPercentage = (scrollTop / (scrollHeight - clientHeight)) * 100;
-
-                const isNearBottom = scrollPercentage >= 95;
-
-                // check if tabelement is active else dont run reloaditems
-                if (isNearBottom && !isLoading && hasMoreitems && tabElement.classList.contains('is-active')) {
+            bindInfiniteScroll({
+                view,
+                tabElement,
+                itemsContainer: tabElement.querySelector('.itemsContainer'),
+                scrollController,
+                shouldLoadMore: () => !isLoading && hasMoreitems,
+                onLoadMore: () => {
                     reloadItems(tabContent, { append: true });
                 }
-            }, { signal: scrollController.signal });
+            });
         }
 
         tabElement.querySelector('.btnShuffle').addEventListener('click', shuffle);
diff --git a/src/controllers/session/login/index.js b/src/controllers/session/login/index.js
index dea26b28d3..b93f574ebd 100644
--- a/src/controllers/session/login/index.js
+++ b/src/controllers/session/login/index.js
@@ -6,6 +6,13 @@ import { ServerConnections } from 'lib/jellyfin-apiclient';
 
 import { appHost } from '../../../components/apphost';
 import appSettings from '../../../scripts/settings/appSettings';
+import {
+    clearRememberedUserToken,
+    getRememberedUser,
+    getRememberedUsers,
+    upsertRememberedUser
+} from '../../../scripts/settings/rememberedUsers';
+import { setUserInfo } from '../../../scripts/settings/userSettings';
 import dom from '../../../utils/dom';
 import loading from '../../../components/loading/loading';
 import layoutManager from '../../../components/layoutManager';
@@ -24,10 +31,26 @@ import './login.scss';
 
 const enableFocusTransform = !browser.slow && !browser.edge;
 
+function isSwitchUserMode(params) {
+    return params.switchUser === '1' || params.switchUser === 'true';
+}
+
+function rememberAuthenticatedUser(apiClient, user, accessToken) {
+    const persistToken = appSettings.enableAutoLogin();
+    upsertRememberedUser(apiClient.serverId(), {
+        userId: user.Id,
+        name: user.Name,
+        primaryImageTag: user.PrimaryImageTag,
+        accessToken: persistToken ? accessToken : null,
+        hasPassword: user.HasPassword
+    }, { persistToken });
+}
+
 function authenticateUserByName(page, apiClient, url, username, password) {
     loading.show();
     apiClient.authenticateUserByName(username, password).then(function (result) {
         const user = result.User;
+        rememberAuthenticatedUser(apiClient, user, result.AccessToken);
         loading.hide();
 
         onLoginSuccessful(user.Id, result.AccessToken, apiClient, url);
@@ -48,6 +71,13 @@ function authenticateUserByName(page, apiClient, url, username, password) {
     });
 }
 
+function closeQuickConnectDialog() {
+    const dlg = document.getElementById('quickConnectAlert');
+    if (dlg) {
+        dialogHelper.close(dlg);
+    }
+}
+
 function authenticateQuickConnect(apiClient, targetUrl) {
     const url = apiClient.getUrl('/QuickConnect/Initiate');
     apiClient.ajax({ type: 'POST', url }, true).then(res => res.json()).then(function (json) {
@@ -74,22 +104,24 @@ function authenticateQuickConnect(apiClient, targetUrl) {
 
                 clearInterval(interval);
 
-                // Close the QuickConnect dialog
-                const dlg = document.getElementById('quickConnectAlert');
-                if (dlg) {
-                    dialogHelper.close(dlg);
+                // Keep the dialog up and hide the login page until navigation completes,
+                // so TVs do not flash the login form between auth and home.
+                loading.show();
+                const loginPage = document.getElementById('loginPage');
+                if (loginPage) {
+                    loginPage.classList.add('hide');
                 }
 
-                const result = await apiClient.quickConnect(data.Secret);
-                onLoginSuccessful(result.User.Id, result.AccessToken, apiClient, targetUrl);
+                try {
+                    const result = await apiClient.quickConnect(data.Secret);
+                    rememberAuthenticatedUser(apiClient, result.User, result.AccessToken);
+                    await onLoginSuccessful(result.User.Id, result.AccessToken, apiClient, targetUrl);
+                } finally {
+                    closeQuickConnectDialog();
+                }
             }, function (e) {
                 clearInterval(interval);
-
-                // Close the QuickConnect dialog
-                const dlg = document.getElementById('quickConnectAlert');
-                if (dlg) {
-                    dialogHelper.close(dlg);
-                }
+                closeQuickConnectDialog();
 
                 Dashboard.alert({
                     message: globalize.translate('QuickConnectDeactivated'),
@@ -114,7 +146,36 @@ function authenticateQuickConnect(apiClient, targetUrl) {
 
 function onLoginSuccessful(id, accessToken, apiClient, url) {
     Dashboard.onServerChanged(id, accessToken, apiClient);
-    Dashboard.navigate(url || 'home');
+    return Dashboard.navigate(url || 'home');
+}
+
+function restoreRememberedUser(apiClient, remembered, targetUrl) {
+    loading.show();
+
+    const server = apiClient.serverInfo() || {};
+    server.UserId = remembered.userId;
+    server.AccessToken = remembered.accessToken;
+
+    const credentialProvider = ServerConnections.credentialProvider();
+    const credentials = credentialProvider.credentials();
+    credentialProvider.addOrUpdateServer(credentials.Servers, server);
+    credentialProvider.credentials(credentials);
+
+    apiClient.setAuthenticationInfo(remembered.accessToken, remembered.userId);
+
+    return apiClient.getCurrentUser().then((user) => {
+        rememberAuthenticatedUser(apiClient, user, remembered.accessToken);
+        return setUserInfo(user.Id, apiClient).then(() => {
+            loading.hide();
+            return onLoginSuccessful(user.Id, remembered.accessToken, apiClient, targetUrl);
+        });
+    }).catch(() => {
+        clearRememberedUserToken(apiClient.serverId(), remembered.userId);
+        apiClient.setAuthenticationInfo(null, null);
+        loading.hide();
+        toast(globalize.translate('MessageInvalidUser'));
+        return false;
+    });
 }
 
 function showManualForm(context, showCancel, focusPassword) {
@@ -136,6 +197,46 @@ function showManualForm(context, showCancel, focusPassword) {
     }
 }
 
+function mergePickerUsers(apiClient, publicUsers) {
+    const remembered = getRememberedUsers(apiClient.serverId());
+    const byId = new Map();
+
+    for (const user of remembered) {
+        byId.set(user.userId, {
+            Id: user.userId,
+            Name: user.name,
+            PrimaryImageTag: user.primaryImageTag,
+            HasPassword: user.hasPassword !== false,
+            AccessToken: user.accessToken
+        });
+    }
+
+    for (const user of publicUsers || []) {
+        if (!byId.has(user.Id)) {
+            byId.set(user.Id, {
+                Id: user.Id,
+                Name: user.Name,
+                PrimaryImageTag: user.PrimaryImageTag,
+                HasPassword: user.HasPassword
+            });
+        } else {
+            const existing = byId.get(user.Id);
+            existing.Name = user.Name || existing.Name;
+            existing.PrimaryImageTag = user.PrimaryImageTag || existing.PrimaryImageTag;
+            existing.HasPassword = user.HasPassword;
+        }
+    }
+
+    return Array.from(byId.values()).sort((a, b) => {
+        const aRemembered = !!a.AccessToken || remembered.some(r => r.userId === a.Id);
+        const bRemembered = !!b.AccessToken || remembered.some(r => r.userId === b.Id);
+        if (aRemembered !== bRemembered) {
+            return aRemembered ? -1 : 1;
+        }
+        return (a.Name || '').localeCompare(b.Name || '');
+    });
+}
+
 function loadUserList(context, apiClient, users) {
     let html = '';
 
@@ -152,11 +253,12 @@ function loadUserList(context, apiClient, users) {
         }
 
         const cardBoxCssClass = 'cardBox cardBox-bottompadded';
+        const hasToken = user.AccessToken ? 'true' : 'false';
         html += '<button type="button" class="' + cssClass + '">';
         html += '<div class="' + cardBoxCssClass + '">';
         html += '<div class="cardScalable">';
         html += '<div class="cardPadder cardPadder-square"></div>';
-        html += `<div class="cardContent" data-haspw="${user.HasPassword}" data-username="${user.Name}" data-userid="${user.Id}">`;
+        html += `<div class="cardContent" data-haspw="${user.HasPassword}" data-username="${user.Name}" data-userid="${user.Id}" data-hastoken="${hasToken}">`;
         let imgUrl;
 
         if (user.PrimaryImageTag) {
@@ -227,12 +329,29 @@ export default function (view, params) {
             const id = cardContent.getAttribute('data-userid');
             const name = cardContent.getAttribute('data-username');
             const haspw = cardContent.getAttribute('data-haspw');
+            const hasToken = cardContent.getAttribute('data-hastoken') === 'true';
+            const apiClient = getApiClient();
 
             if (id === 'manual') {
                 context.querySelector('#txtManualName').value = '';
                 showManualForm(context, true);
+            } else if (hasToken) {
+                const remembered = getRememberedUser(apiClient.serverId(), id);
+                if (remembered?.accessToken) {
+                    restoreRememberedUser(apiClient, remembered, getTargetUrl()).then((ok) => {
+                        if (ok === false) {
+                            if (haspw == 'false') {
+                                authenticateUserByName(context, apiClient, getTargetUrl(), name, '');
+                            } else {
+                                context.querySelector('#txtManualName').value = name;
+                                context.querySelector('#txtManualPassword').value = '';
+                                showManualForm(context, true, true);
+                            }
+                        }
+                    });
+                }
             } else if (haspw == 'false') {
-                authenticateUserByName(context, getApiClient(), getTargetUrl(), name, '');
+                authenticateUserByName(context, apiClient, getTargetUrl(), name, '');
             } else {
                 context.querySelector('#txtManualName').value = name;
                 context.querySelector('#txtManualPassword').value = '';
@@ -265,12 +384,14 @@ export default function (view, params) {
     view.addEventListener('viewshow', function () {
         loading.show();
         libraryMenu.setTransparentMenu(true);
+        view.classList.remove('hide');
 
         if (!appHost.supports(AppFeature.MultiServer)) {
             view.querySelector('.btnSelectServer').classList.add('hide');
         }
 
         const apiClient = getApiClient();
+        const switchUserMode = isSwitchUserMode(params);
 
         let autoQuickConnectStarted = false;
 
@@ -280,8 +401,9 @@ export default function (view, params) {
                     view.querySelector('.btnQuick').classList.remove('hide');
 
                     // TV remotes struggle with username/password (and SSO IdP UIs).
-                    // Auto-start Quick Connect once per login view when on a TV client.
-                    if (browser.tv && !autoQuickConnectStarted) {
+                    // Auto-start Quick Connect once per login view when on a TV client,
+                    // but not when switching profiles on a shared device.
+                    if (browser.tv && !autoQuickConnectStarted && !switchUserMode) {
                         autoQuickConnectStarted = true;
                         authenticateQuickConnect(apiClient, getTargetUrl());
                     }
@@ -291,7 +413,8 @@ export default function (view, params) {
                 console.debug('Failed to get QuickConnect status');
             });
 
-        apiClient.getPublicUsers().then(function (users) {
+        apiClient.getPublicUsers().then(function (publicUsers) {
+            const users = mergePickerUsers(apiClient, publicUsers);
             if (users.length) {
                 showVisualForm();
                 loadUserList(view, apiClient, users);
@@ -325,4 +448,3 @@ export default function (view, params) {
         libraryMenu.setTransparentMenu(false);
     });
 }
-
diff --git a/src/controllers/shows/episodes.js b/src/controllers/shows/episodes.js
index f34c789e57..9ee020de65 100644
--- a/src/controllers/shows/episodes.js
+++ b/src/controllers/shows/episodes.js
@@ -4,6 +4,7 @@ import imageLoader from '../../components/images/imageLoader';
 import listView from '../../components/listview/listview';
 import cardBuilder from '../../components/cardbuilder/cardBuilder';
 import * as userSettings from '../../scripts/settings/userSettings';
+import { bindInfiniteScroll, getInfiniteScrollLimit } from '../../scripts/infiniteScrollHelper';
 import globalize from '../../lib/globalize';
 import Dashboard from '../../utils/dashboard';
 import Events from '../../utils/events.ts';
@@ -72,11 +73,7 @@ export default function (view, params, tabContent) {
         loading.show();
         isLoading = true;
         const query = getQuery();
-        if (userSettings.enableInfiniteScroll() && userSettings.libraryPageSize() < 40) {
-            query.Limit = 40;
-        } else {
-            query.Limit = userSettings.libraryPageSize();
-        }
+        query.Limit = getInfiniteScrollLimit(40);
 
         setFilterStatus(page, query);
 
@@ -174,7 +171,7 @@ export default function (view, params, tabContent) {
                 }
             }
             if (append) {
-                itemsContainer.innerHTML += html;
+                itemsContainer.insertAdjacentHTML('beforeend', html);
             } else {
                 itemsContainer.innerHTML = html;
             }
@@ -183,9 +180,11 @@ export default function (view, params, tabContent) {
             loading.hide();
             isLoading = false;
 
-            import('../../components/autoFocuser').then(({ default: autoFocuser }) => {
-                autoFocuser.autoFocus(page);
-            });
+            if (!append) {
+                import('../../components/autoFocuser').then(({ default: autoFocuser }) => {
+                    autoFocuser.autoFocus(page);
+                });
+            }
         });
     }
 
@@ -253,23 +252,16 @@ export default function (view, params, tabContent) {
         });
 
         if (userSettings.enableInfiniteScroll()) {
-            view.addEventListener('viewdestroy', () => {
-                scrollController.abort();
-            }, { signal: scrollController.signal });
-
-            window.addEventListener('scroll', () => {
-                const scrollTop = window.scrollY || window.pageYOffset;
-                const scrollHeight = document.documentElement.scrollHeight || document.body.scrollHeight;
-                const clientHeight = document.documentElement.clientHeight || window.innerHeight;
-                const scrollPercentage = (scrollTop / (scrollHeight - clientHeight)) * 100;
-
-                const isNearBottom = scrollPercentage >= 95;
-
-                // check if tabelement is active else dont run reloaditems
-                if (isNearBottom && !isLoading && hasMoreitems && tabElement.classList.contains('is-active')) {
+            bindInfiniteScroll({
+                view,
+                tabElement,
+                itemsContainer: tabElement.querySelector('.itemsContainer'),
+                scrollController,
+                shouldLoadMore: () => !isLoading && hasMoreitems,
+                onLoadMore: () => {
                     reloadItems(tabContent, { append: true });
                 }
-            }, { signal: scrollController.signal });
+            });
         }
 
         const btnSelectView = tabElement.querySelector('.btnSelectView');
diff --git a/src/controllers/shows/tvshows.js b/src/controllers/shows/tvshows.js
index 4a7feee266..bac98aa43f 100644
--- a/src/controllers/shows/tvshows.js
+++ b/src/controllers/shows/tvshows.js
@@ -5,6 +5,7 @@ import listView from '../../components/listview/listview';
 import cardBuilder from '../../components/cardbuilder/cardBuilder';
 import AlphaPicker from '../../components/alphaPicker/alphaPicker';
 import * as userSettings from '../../scripts/settings/userSettings';
+import { bindInfiniteScroll, getInfiniteScrollLimit } from '../../scripts/infiniteScrollHelper';
 import globalize from '../../lib/globalize';
 import Events from '../../utils/events.ts';
 import { setFilterStatus } from 'components/filterdialog/filterIndicator';
@@ -71,11 +72,7 @@ export default function (view, params, tabContent) {
         loading.show();
         isLoading = true;
         const query = getQuery();
-        if (userSettings.enableInfiniteScroll() && userSettings.libraryPageSize() < 40) {
-            query.Limit = 40;
-        } else {
-            query.Limit = userSettings.libraryPageSize();
-        }
+        query.Limit = getInfiniteScrollLimit(40);
 
         setFilterStatus(page, query);
 
@@ -216,7 +213,7 @@ export default function (view, params, tabContent) {
 
             const itemsContainer = tabContent.querySelector('.itemsContainer');
             if (append) {
-                itemsContainer.innerHTML += html;
+                itemsContainer.insertAdjacentHTML('beforeend', html);
             } else {
                 itemsContainer.innerHTML = html;
             }
@@ -225,9 +222,11 @@ export default function (view, params, tabContent) {
             loading.hide();
             isLoading = false;
 
-            import('../../components/autoFocuser').then(({ default: autoFocuser }) => {
-                autoFocuser.autoFocus(page);
-            });
+            if (!append) {
+                import('../../components/autoFocuser').then(({ default: autoFocuser }) => {
+                    autoFocuser.autoFocus(page);
+                });
+            }
         });
     };
 
@@ -333,23 +332,16 @@ export default function (view, params, tabContent) {
         });
 
         if (userSettings.enableInfiniteScroll()) {
-            view.addEventListener('viewdestroy', () => {
-                scrollController.abort();
-            }, { signal: scrollController.signal });
-
-            window.addEventListener('scroll', () => {
-                const scrollTop = window.scrollY || window.pageYOffset;
-                const scrollHeight = document.documentElement.scrollHeight || document.body.scrollHeight;
-                const clientHeight = document.documentElement.clientHeight || window.innerHeight;
-                const scrollPercentage = (scrollTop / (scrollHeight - clientHeight)) * 100;
-
-                const isNearBottom = scrollPercentage >= 95;
-
-                // check if tabelement is active else dont run reloaditems
-                if (isNearBottom && !isLoading && hasMoreitems && tabElement.classList.contains('is-active')) {
+            bindInfiniteScroll({
+                view,
+                tabElement,
+                itemsContainer,
+                scrollController,
+                shouldLoadMore: () => !isLoading && hasMoreitems,
+                onLoadMore: () => {
                     reloadItems(tabContent, { append: true });
                 }
-            }, { signal: scrollController.signal });
+            });
         }
     };
 
diff --git a/src/elements/emby-itemscontainer/emby-itemscontainer.js b/src/elements/emby-itemscontainer/emby-itemscontainer.js
index a6e4158e8c..0692e56078 100644
--- a/src/elements/emby-itemscontainer/emby-itemscontainer.js
+++ b/src/elements/emby-itemscontainer/emby-itemscontainer.js
@@ -445,7 +445,14 @@ function onDataFetched(result) {
         focusId = activeElement.getAttribute('data-id');
     }
 
-    this.innerHTML = this.getItemsHtml(items);
+    const html = this.getItemsHtml(items);
+    if (this.appendOnRefresh && html) {
+        // Append-only refresh avoids rewriting the entire card tree (critical on slow TVs).
+        this.insertAdjacentHTML('beforeend', html);
+    } else {
+        this.innerHTML = html || '';
+    }
+    this.appendOnRefresh = false;
 
     imageLoader.lazyChildren(this);
 
diff --git a/src/scripts/infiniteScrollHelper.js b/src/scripts/infiniteScrollHelper.js
new file mode 100644
index 0000000000..d6b3192fe8
--- /dev/null
+++ b/src/scripts/infiniteScrollHelper.js
@@ -0,0 +1,101 @@
+import browser from './browser';
+import * as userSettings from './settings/userSettings';
+
+/**
+ * Minimum page size for infinite scroll. TVs need larger chunks to avoid
+ * frequent DOM updates on slow Chromium builds (e.g. webOS 5).
+ * @param {number} desktopMin
+ * @returns {number}
+ */
+export function getInfiniteScrollLimit(desktopMin = 40) {
+    const pageSize = userSettings.libraryPageSize();
+    const minLimit = (browser.tv || browser.slow) ? 100 : desktopMin;
+
+    if (userSettings.enableInfiniteScroll()) {
+        if (!pageSize || pageSize < minLimit) {
+            return minLimit;
+        }
+    }
+
+    return pageSize;
+}
+
+/**
+ * @returns {number} percent of scroll height at which to prefetch
+ */
+export function getScrollPrefetchThreshold() {
+    return (browser.tv || browser.slow) ? 70 : 95;
+}
+
+/**
+ * @param {number} [threshold]
+ * @returns {boolean}
+ */
+export function isNearScrollBottom(threshold = getScrollPrefetchThreshold()) {
+    const scrollTop = window.scrollY || window.pageYOffset;
+    const scrollHeight = document.documentElement.scrollHeight || document.body.scrollHeight;
+    const clientHeight = document.documentElement.clientHeight || window.innerHeight;
+    const denom = scrollHeight - clientHeight;
+    if (denom <= 0) {
+        return false;
+    }
+
+    return ((scrollTop / denom) * 100) >= threshold;
+}
+
+/**
+ * Bind window scroll + focus-near-end listeners for infinite scroll.
+ * @param {{
+ *   view: Element,
+ *   tabElement?: Element,
+ *   itemsContainer: Element,
+ *   scrollController: AbortController,
+ *   shouldLoadMore: () => boolean,
+ *   onLoadMore: () => void
+ * }} options
+ */
+export function bindInfiniteScroll(options) {
+    const {
+        view,
+        tabElement,
+        itemsContainer,
+        scrollController,
+        shouldLoadMore,
+        onLoadMore
+    } = options;
+
+    view.addEventListener('viewdestroy', () => {
+        scrollController.abort();
+    }, { signal: scrollController.signal });
+
+    const maybeLoad = () => {
+        if (!shouldLoadMore()) {
+            return;
+        }
+
+        if (tabElement && !tabElement.classList.contains('is-active')) {
+            return;
+        }
+
+        onLoadMore();
+    };
+
+    window.addEventListener('scroll', () => {
+        if (isNearScrollBottom()) {
+            maybeLoad();
+        }
+    }, { signal: scrollController.signal, passive: true });
+
+    itemsContainer.addEventListener('focusin', (e) => {
+        const cards = itemsContainer.querySelectorAll('[data-id]');
+        if (!cards.length) {
+            return;
+        }
+
+        const lookback = Math.min(12, cards.length);
+        const lastCards = Array.from(cards).slice(-lookback);
+        if (lastCards.some(card => card === e.target || card.contains(e.target))) {
+            maybeLoad();
+        }
+    }, { signal: scrollController.signal });
+}
diff --git a/src/scripts/libraryMenu.js b/src/scripts/libraryMenu.js
index 60a22d3408..43a0ad81d0 100644
--- a/src/scripts/libraryMenu.js
+++ b/src/scripts/libraryMenu.js
@@ -351,6 +351,7 @@ function refreshLibraryInfoInDrawer(user) {
         }
 
         html += `<a is="emby-linkbutton" class="navMenuOption lnkMediaFolder btnSettings" data-itemid="settings" href="#"><span class="material-icons navMenuOptionIcon settings" aria-hidden="true"></span><span class="navMenuOptionText">${globalize.translate('Settings')}</span></a>`;
+        html += `<a is="emby-linkbutton" class="navMenuOption lnkMediaFolder btnSwitchUser" data-itemid="switchuser" href="#"><span class="material-icons navMenuOptionIcon people" aria-hidden="true"></span><span class="navMenuOptionText">${globalize.translate('ButtonSwitchUser')}</span></a>`;
         html += `<a is="emby-linkbutton" class="navMenuOption lnkMediaFolder btnLogout" data-itemid="logout" href="#"><span class="material-icons navMenuOptionIcon exit_to_app" aria-hidden="true"></span><span class="navMenuOptionText">${globalize.translate('ButtonSignOut')}</span></a>`;
 
         if (appHost.supports(AppFeature.ExitMenu)) {
@@ -378,6 +379,11 @@ function refreshLibraryInfoInDrawer(user) {
         btnExit.addEventListener('click', onExitAppClick);
     }
 
+    const btnSwitchUser = navDrawerScrollContainer.querySelector('.btnSwitchUser');
+    if (btnSwitchUser) {
+        btnSwitchUser.addEventListener('click', onSwitchUserClick);
+    }
+
     const btnLogout = navDrawerScrollContainer.querySelector('.btnLogout');
     if (btnLogout) {
         btnLogout.addEventListener('click', onLogoutClick);
@@ -511,6 +517,10 @@ function onExitAppClick() {
     appHost.exit();
 }
 
+function onSwitchUserClick() {
+    Dashboard.switchUser();
+}
+
 function onLogoutClick() {
     Dashboard.logout();
 }
diff --git a/src/scripts/settings/rememberedUsers.js b/src/scripts/settings/rememberedUsers.js
new file mode 100644
index 0000000000..285da85792
--- /dev/null
+++ b/src/scripts/settings/rememberedUsers.js
@@ -0,0 +1,114 @@
+const STORAGE_KEY = 'jellyfin_remembered_users';
+const MAX_USERS_PER_SERVER = 8;
+
+function readStore() {
+    try {
+        const raw = localStorage.getItem(STORAGE_KEY);
+        if (!raw) {
+            return {};
+        }
+
+        const parsed = JSON.parse(raw);
+        return parsed && typeof parsed === 'object' ? parsed : {};
+    } catch {
+        return {};
+    }
+}
+
+function writeStore(store) {
+    localStorage.setItem(STORAGE_KEY, JSON.stringify(store));
+}
+
+function normalizeServerId(serverId) {
+    return serverId || '_default';
+}
+
+/**
+ * @param {string} serverId
+ * @returns {Array<{userId: string, name: string, primaryImageTag?: string, lastSignedIn: number, accessToken?: string, hasPassword?: boolean}>}
+ */
+export function getRememberedUsers(serverId) {
+    const list = readStore()[normalizeServerId(serverId)] || [];
+    return list
+        .slice()
+        .sort((a, b) => (b.lastSignedIn || 0) - (a.lastSignedIn || 0));
+}
+
+/**
+ * Upsert a remembered profile for a server.
+ * @param {string} serverId
+ * @param {{userId: string, name: string, primaryImageTag?: string, accessToken?: string|null, hasPassword?: boolean}} profile
+ * @param {{persistToken?: boolean}} [options]
+ */
+export function upsertRememberedUser(serverId, profile, options = {}) {
+    if (!profile?.userId || !profile?.name) {
+        return;
+    }
+
+    const key = normalizeServerId(serverId);
+    const store = readStore();
+    const list = store[key] || [];
+    const existing = list.find(u => u.userId === profile.userId);
+    const persistToken = options.persistToken !== false;
+
+    const next = {
+        userId: profile.userId,
+        name: profile.name,
+        primaryImageTag: profile.primaryImageTag || existing?.primaryImageTag,
+        hasPassword: profile.hasPassword ?? existing?.hasPassword ?? true,
+        lastSignedIn: Date.now()
+    };
+
+    if (profile.accessToken === null) {
+        // Explicitly drop token.
+    } else if (persistToken && profile.accessToken) {
+        next.accessToken = profile.accessToken;
+    } else if (persistToken && existing?.accessToken) {
+        next.accessToken = existing.accessToken;
+    }
+
+    const filtered = list.filter(u => u.userId !== profile.userId);
+    filtered.unshift(next);
+    store[key] = filtered.slice(0, MAX_USERS_PER_SERVER);
+    writeStore(store);
+}
+
+/**
+ * Clear only the stored access token for a user (keeps picker entry).
+ * @param {string} serverId
+ * @param {string} userId
+ */
+export function clearRememberedUserToken(serverId, userId) {
+    const key = normalizeServerId(serverId);
+    const store = readStore();
+    const list = store[key];
+    if (!list) {
+        return;
+    }
+
+    store[key] = list.map(u => {
+        if (u.userId !== userId) {
+            return u;
+        }
+
+        const copy = { ...u };
+        delete copy.accessToken;
+        return copy;
+    });
+    writeStore(store);
+}
+
+/**
+ * @param {string} serverId
+ * @param {string} userId
+ */
+export function getRememberedUser(serverId, userId) {
+    return getRememberedUsers(serverId).find(u => u.userId === userId) || null;
+}
+
+export default {
+    getRememberedUsers,
+    getRememberedUser,
+    upsertRememberedUser,
+    clearRememberedUserToken
+};
diff --git a/src/scripts/settings/userSettings.js b/src/scripts/settings/userSettings.js
index dec77a630e..7b9babf816 100644
--- a/src/scripts/settings/userSettings.js
+++ b/src/scripts/settings/userSettings.js
@@ -282,7 +282,9 @@ export class UserSettings {
             return this.set('fastFadein', val.toString(), false);
         }
 
-        return toBoolean(this.get('fastFadein', false), true);
+        // TVs (especially webOS) struggle with extra animation work.
+        const defaultEnabled = !(browser.tv || browser.web0s);
+        return toBoolean(this.get('fastFadein', false), defaultEnabled);
     }
 
     /**
@@ -295,7 +297,9 @@ export class UserSettings {
             return this.set('blurhash', val.toString(), false);
         }
 
-        return toBoolean(this.get('blurhash', false), true);
+        // Blurhash decoding is expensive on constrained TV Chromium builds.
+        const defaultEnabled = !(browser.tv || browser.web0s);
+        return toBoolean(this.get('blurhash', false), defaultEnabled);
     }
 
     /**
diff --git a/src/strings/en-us.json b/src/strings/en-us.json
index 7daf329bed..89f48bd698 100755
--- a/src/strings/en-us.json
+++ b/src/strings/en-us.json
@@ -143,6 +143,7 @@
     "ButtonShutdown": "Shutdown",
     "ButtonSignIn": "Sign In",
     "ButtonSignOut": "Sign Out",
+    "ButtonSwitchUser": "Switch User",
     "ButtonExitApp": "Exit Application",
     "ButtonSpace": "Space",
     "ButtonSplit": "Split",
diff --git a/src/utils/dashboard.js b/src/utils/dashboard.js
index b7deb9f8d0..abf4a0d387 100644
--- a/src/utils/dashboard.js
+++ b/src/utils/dashboard.js
@@ -15,6 +15,11 @@ import { setBackdropTransparency } from '../components/backdrop/backdrop';
 import DirectoryBrowser from '../components/directorybrowser/directorybrowser';
 import dialogHelper from '../components/dialogHelper/dialogHelper';
 import itemIdentifier from '../components/itemidentifier/itemidentifier';
+import {
+    clearRememberedUserToken,
+    upsertRememberedUser
+} from '../scripts/settings/rememberedUsers';
+import { setUserInfo } from '../scripts/settings/userSettings';
 import { getLocationSearch } from './url.ts';
 import { queryClient } from './query/queryClient';
 
@@ -102,7 +107,16 @@ export function onServerChanged(_userId, _accessToken, apiClient) {
 }
 
 export function logout() {
+    const apiClient = window.ApiClient;
+    const serverId = apiClient?.serverId?.();
+    const userId = apiClient?.getCurrentUserId?.();
+
     ServerConnections.logout().then(function () {
+        // Server logout invalidates the token; keep the picker entry without it.
+        if (serverId && userId) {
+            clearRememberedUserToken(serverId, userId);
+        }
+
         // Clear the query cache
         queryClient.clear();
         // Reset cached views
@@ -112,6 +126,63 @@ export function logout() {
     });
 }
 
+/**
+ * Soft sign-out for shared-device profile switching.
+ * Keeps remembered profile tokens valid (does not call server logout).
+ */
+export function switchUser() {
+    const apiClient = window.ApiClient;
+    const finish = (serverId) => {
+        queryClient.clear();
+        viewContainer.reset();
+        const loginUrl = serverId
+            ? `login?serverid=${encodeURIComponent(serverId)}&switchUser=1`
+            : 'login?switchUser=1';
+        return navigate(loginUrl);
+    };
+
+    if (!apiClient) {
+        return Promise.resolve(finish());
+    }
+
+    const serverId = apiClient.serverId();
+    const accessToken = apiClient.accessToken();
+
+    return apiClient.getCurrentUser().then((user) => {
+        upsertRememberedUser(serverId, {
+            userId: user.Id,
+            name: user.Name,
+            primaryImageTag: user.PrimaryImageTag,
+            accessToken,
+            hasPassword: user.HasPassword
+        });
+    }).catch(() => {
+        const userId = apiClient.getCurrentUserId();
+        if (userId) {
+            upsertRememberedUser(serverId, {
+                userId,
+                name: userId,
+                accessToken
+            });
+        }
+    }).then(() => {
+        const server = apiClient.serverInfo() || {};
+        server.UserId = null;
+        server.AccessToken = null;
+        server.ExchangeToken = null;
+
+        const credentialProvider = ServerConnections.credentialProvider();
+        const credentials = credentialProvider.credentials();
+        credentialProvider.addOrUpdateServer(credentials.Servers, server);
+        credentialProvider.credentials(credentials);
+
+        apiClient.setAuthenticationInfo(null, null);
+        setUserInfo(null, null);
+
+        return finish(serverId);
+    });
+}
+
 export function getPluginUrl(name) {
     return 'configurationpage?name=' + encodeURIComponent(name);
 }
@@ -243,6 +314,7 @@ const Dashboard = {
     getCurrentUserId,
     hideLoadingMsg,
     logout,
+    switchUser,
     navigate,
     onServerChanged,
     processErrorResponse,
