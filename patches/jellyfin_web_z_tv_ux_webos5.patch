diff --git a/src/apps/stable/features/search/api/useSeerrSearch.ts b/src/apps/stable/features/search/api/useSeerrSearch.ts
index 2f992a03d3..27d400015b 100644
--- a/src/apps/stable/features/search/api/useSeerrSearch.ts
+++ b/src/apps/stable/features/search/api/useSeerrSearch.ts
@@ -6,16 +6,18 @@ import {
     requestSeerrMedia
 } from 'lib/seerr/seerrApi';
 import { isSeerrEnabled } from 'lib/seerr/seerrTypes';
+import { isTvClient } from 'scripts/browser';
 
 export { requestSeerrMedia };
 
 export const useSeerrStatus = () => {
     const { __legacyApiClient__: apiClient } = useApi();
+    const seerrAllowed = !isTvClient();
 
     return useQuery({
         queryKey: ['Seerr', 'Status'],
         queryFn: () => fetchSeerrStatus(apiClient!),
-        enabled: !!apiClient,
+        enabled: seerrAllowed && !!apiClient,
         staleTime: 60_000,
         retry: false
     });
@@ -25,11 +27,12 @@ export const useSeerrSearch = (searchTerm?: string) => {
     const { __legacyApiClient__: apiClient } = useApi();
     const { data: status } = useSeerrStatus();
     const query = searchTerm?.trim();
+    const seerrAllowed = !isTvClient();
 
     return useQuery({
         queryKey: ['Seerr', 'Search', query],
         queryFn: () => fetchSeerrSearch(apiClient!, query!),
-        enabled: !!apiClient && isSeerrEnabled(status) && !!query,
+        enabled: seerrAllowed && !!apiClient && isSeerrEnabled(status) && !!query,
         retry: false
     });
 };
diff --git a/src/apps/stable/features/search/components/SearchResults.tsx b/src/apps/stable/features/search/components/SearchResults.tsx
index 093f59760a..2417cef922 100644
--- a/src/apps/stable/features/search/components/SearchResults.tsx
+++ b/src/apps/stable/features/search/components/SearchResults.tsx
@@ -10,6 +10,7 @@ import { CardShape } from 'components/cardbuilder/utils/shape';
 import { CollectionType } from '@jellyfin/sdk/lib/generated-client/models/collection-type';
 import { Section } from '../types';
 import { Link } from 'react-router-dom';
+import { isTvClient } from 'scripts/browser';
 import * as userSettings from 'scripts/settings/userSettings';
 
 interface SearchResultsProps {
@@ -34,8 +35,13 @@ const SearchResults: FC<SearchResultsProps> = ({
         hasNextPage,
         isFetchingNextPage
     } = useSearchItems(parentId, collectionType, trimmedQuery);
-    const { data: seerrData, isPending: isSeerrPending } = useSeerrSearch(trimmedQuery);
-    const seerrItems = getSeerrSearchItems(seerrData).filter(item => item.canRequest);
+    const seerrOnTv = isTvClient();
+    const { data: seerrData, isPending: isSeerrPending } = useSeerrSearch(
+        seerrOnTv ? undefined : trimmedQuery
+    );
+    const seerrItems = seerrOnTv
+        ? []
+        : getSeerrSearchItems(seerrData).filter(item => item.canRequest);
 
     useEffect(() => {
         if (!userSettings.enableInfiniteScroll()) {
@@ -72,7 +78,7 @@ const SearchResults: FC<SearchResultsProps> = ({
 
     if (isPending) return <Loading />;
 
-    if (!data?.length && !seerrItems.length && !isSeerrPending) {
+    if (!data?.length && !seerrItems.length && (seerrOnTv || !isSeerrPending)) {
         return (
             <div className='noItemsMessage centerMessage'>
                 {globalize.translate('SearchResultsEmpty', query)}
@@ -110,7 +116,7 @@ const SearchResults: FC<SearchResultsProps> = ({
     return (
         <div className={'searchResults padded-top padded-bottom-page'}>
             {data?.map((section, index) => renderSection(section, index))}
-            {!isSeerrPending && seerrItems.length > 0 && (
+            {!seerrOnTv && !isSeerrPending && seerrItems.length > 0 && (
                 <SeerrRequestResults items={seerrItems} />
             )}
             {isFetchingNextPage && <Loading />}
diff --git a/src/apps/stable/routes/routes.tsx b/src/apps/stable/routes/routes.tsx
index 3174798293..5cc21a21e0 100644
--- a/src/apps/stable/routes/routes.tsx
+++ b/src/apps/stable/routes/routes.tsx
@@ -6,11 +6,25 @@ import { toAsyncPageRoute } from 'components/router/AsyncRoute';
 import { toViewManagerPageRoute } from 'components/router/LegacyRoute';
 import ErrorBoundary from 'components/router/ErrorBoundary';
 import FallbackRoute from 'components/router/FallbackRoute';
+import { isTvClient } from 'scripts/browser';
 
 import AppLayout from '../AppLayout';
 
 import { ASYNC_PUBLIC_ROUTES, ASYNC_USER_ROUTES } from './asyncRoutes';
 import { LEGACY_PUBLIC_ROUTES, LEGACY_USER_ROUTES } from './legacyRoutes';
+// Eager search on TV avoids Chromium 68 / webOS ChunkLoadError from lazy chunks.
+import SearchPage from './search';
+
+const tvClient = isTvClient();
+
+const asyncUserRoutes = (tvClient
+    ? ASYNC_USER_ROUTES.filter((route) => route.path !== 'search')
+    : ASYNC_USER_ROUTES
+).map(toAsyncPageRoute);
+
+const tvSearchRoutes: RouteObject[] = tvClient
+    ? [{ path: 'search', Component: SearchPage }]
+    : [];
 
 export const STABLE_APP_ROUTES: RouteObject[] = [
     {
@@ -23,7 +37,8 @@ export const STABLE_APP_ROUTES: RouteObject[] = [
                 /* User routes */
                 Component: ConnectionRequired,
                 children: [
-                    ...ASYNC_USER_ROUTES.map(toAsyncPageRoute),
+                    ...tvSearchRoutes,
+                    ...asyncUserRoutes,
                     ...LEGACY_USER_ROUTES.map(toViewManagerPageRoute)
                 ],
                 ErrorBoundary
diff --git a/src/components/homesections/homesections.js b/src/components/homesections/homesections.js
index df1a88c10e..8953492993 100644
--- a/src/components/homesections/homesections.js
+++ b/src/components/homesections/homesections.js
@@ -2,6 +2,7 @@ import layoutManager from 'components/layoutManager';
 import { DEFAULT_SECTIONS, HomeSectionType } from 'constants/homeSectionType';
 import { getUserViewsQuery } from 'hooks/api/useUserViews';
 import globalize from 'lib/globalize';
+import browser, { isTvClient } from 'scripts/browser';
 import Dashboard from 'utils/dashboard';
 import { toApi } from 'utils/jellyfin-apiclient/compat';
 import { queryClient } from 'utils/query/queryClient';
@@ -24,8 +25,10 @@ import 'elements/emby-button/emby-button';
 import './homesections.scss';
 
 const MAX_SECTIONS = 10;
-/** Cap active home rows on TV to limit API + image pressure on low-RAM clients (webOS). */
-const MAX_ACTIVE_SECTIONS_TV = 4;
+
+function isConstrainedTv() {
+    return isTvClient() || !!(browser.tv || browser.web0s) || layoutManager.tv;
+}
 
 export function getDefaultSection(index) {
     if (index < 0 || index > DEFAULT_SECTIONS.length) return '';
@@ -33,6 +36,28 @@ export function getDefaultSection(index) {
 }
 
 function getAllSectionsToShow(userSettings) {
+    if (isConstrainedTv()) {
+        const configured = [];
+        for (let i = 0, length = MAX_SECTIONS; i < length; i++) {
+            let section = userSettings.get('homesection' + i) || getDefaultSection(i);
+            if (section === 'folders') {
+                section = getDefaultSection(0);
+            }
+            configured.push(section);
+        }
+
+        const librarySection = configured.includes(HomeSectionType.LibraryButtons)
+            ? HomeSectionType.LibraryButtons
+            : HomeSectionType.SmallLibraryTiles;
+
+        return [
+            librarySection,
+            HomeSectionType.Resume,
+            HomeSectionType.NextUp,
+            HomeSectionType.LatestMedia
+        ];
+    }
+
     const sections = [];
     for (let i = 0, length = MAX_SECTIONS; i < length; i++) {
         let section = userSettings.get('homesection' + i) || getDefaultSection(i);
@@ -43,20 +68,6 @@ function getAllSectionsToShow(userSettings) {
         sections.push(section);
     }
 
-    // Ensure libraries are visible in TV layout
-    if (
-        layoutManager.tv
-            && !sections.includes(HomeSectionType.SmallLibraryTiles)
-            && !sections.includes(HomeSectionType.LibraryButtons)
-    ) {
-        sections.unshift(HomeSectionType.SmallLibraryTiles);
-    }
-
-    if (layoutManager.tv) {
-        const active = sections.filter((section) => section && section !== HomeSectionType.None);
-        return active.slice(0, MAX_ACTIVE_SECTIONS_TV);
-    }
-
     return sections;
 }
 
@@ -70,7 +81,7 @@ export function loadSections(elem, apiClient, user, userSettings) {
 
             if (userViews.length) {
                 const sectionsToShow = getAllSectionsToShow(userSettings);
-                const totalSectionCount = layoutManager.tv
+                const totalSectionCount = isConstrainedTv()
                     ? sectionsToShow.length
                     : MAX_SECTIONS;
                 for (let i = 0; i < totalSectionCount; i++) {
diff --git a/src/components/homesections/sections/beyondYourLibrary.ts b/src/components/homesections/sections/beyondYourLibrary.ts
index 49e354cd16..06642edebf 100644
--- a/src/components/homesections/sections/beyondYourLibrary.ts
+++ b/src/components/homesections/sections/beyondYourLibrary.ts
@@ -14,6 +14,7 @@ import {
 } from 'lib/seerr/seerrTypes';
 import { mapGenreLabelsToTmdbIds } from 'lib/seerr/tmdbGenres';
 import globalize from 'lib/globalize';
+import { isTvClient } from 'scripts/browser';
 
 import type { SectionOptions } from './section';
 
@@ -224,6 +225,12 @@ export async function loadBeyondYourLibrary(
     elem.classList.remove('verticalSection');
     elem.innerHTML = '';
 
+    // Skip Seerr discover rows on constrained TV clients (webOS / Chromium 68).
+    if (isTvClient()) {
+        elem.classList.add('hide');
+        return;
+    }
+
     try {
         const status = await fetchSeerrStatus(apiClient);
         if (!isSeerrEnabled(status)) {
diff --git a/src/scripts/browser.js b/src/scripts/browser.js
index b020d197db..0f12334f7f 100644
--- a/src/scripts/browser.js
+++ b/src/scripts/browser.js
@@ -344,7 +344,17 @@ export const detectBrowser = (userAgent = navigator.userAgent) => {
         }
     }
 
+    // Constrained TV clients (LG webOS, etc.) need lighter UI paths.
+    browser.isTvClient = !!(browser.tv || browser.web0s);
+
     return browser;
 };
 
-export default detectBrowser();
+const browserInstance = detectBrowser();
+
+/** @returns {boolean} */
+export function isTvClient() {
+    return !!browserInstance.isTvClient;
+}
+
+export default browserInstance;
diff --git a/src/scripts/settings/userSettings.js b/src/scripts/settings/userSettings.js
index 7b9babf816..f8c01361b6 100644
--- a/src/scripts/settings/userSettings.js
+++ b/src/scripts/settings/userSettings.js
@@ -7,7 +7,7 @@ import { toApi } from 'utils/jellyfin-apiclient/compat';
 import { queryClient } from 'utils/query/queryClient';
 import { toBoolean } from 'utils/string';
 
-import browser from '../browser';
+import browser, { isTvClient } from '../browser';
 import appSettings from './appSettings';
 
 const DISPLAY_PREFERENCES_ID = 'usersettings';
@@ -539,6 +539,11 @@ export class UserSettings {
             return this.set('enableInfiniteScroll', val.toString(), false);
         }
 
+        // Chromium 68 / webOS: force classic paging — never grow unbounded card trees.
+        if (isTvClient() || browser.tv || browser.web0s) {
+            return false;
+        }
+
         return toBoolean(this.get('enableInfiniteScroll', false), false);
     }
 
