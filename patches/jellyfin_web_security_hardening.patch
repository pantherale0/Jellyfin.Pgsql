diff --git a/src/components/itemContextMenu.js b/src/components/itemContextMenu.js
index 53e09fcf24..998bbb5e01 100644
--- a/src/components/itemContextMenu.js
+++ b/src/components/itemContextMenu.js
@@ -482,13 +482,26 @@ function executeCommand(item, id, options) {
                 break;
             }
             case 'copy-stream': {
-                const downloadHref = getLibraryApi(api).getDownloadUrl({ itemId });
-                copy(downloadHref).then(() => {
-                    toast(globalize.translate('CopyStreamURLSuccess'));
+                // Mint an item-scoped, expiring share token instead of embedding the session ApiKey.
+                const expiresInSeconds = 86400;
+                apiClient.ajax({
+                    type: 'POST',
+                    url: apiClient.getUrl(`Items/${itemId}/ShareLinks`),
+                    data: JSON.stringify({ expiresInSeconds }),
+                    contentType: 'application/json',
+                    dataType: 'json'
+                }).then((result) => {
+                    const shareUrl = result.Url || result.url;
+                    return copy(shareUrl).then(() => {
+                        toast(globalize.translate('CopyStreamURLSuccess'));
+                    }).catch(() => {
+                        prompt(globalize.translate('CopyStreamURL'), shareUrl);
+                    });
                 }).catch(() => {
-                    prompt(globalize.translate('CopyStreamURL'), downloadHref);
+                    toast(globalize.translate('CopyStreamURLError'));
+                }).finally(() => {
+                    getResolveFunction(resolve, id)();
                 });
-                getResolveFunction(resolve, id)();
                 break;
             }
             case 'editsubtitles':
diff --git a/src/lib/jellyfin-apiclient/ServerConnections.js b/src/lib/jellyfin-apiclient/ServerConnections.js
index c88e038478..e0b1380086 100644
--- a/src/lib/jellyfin-apiclient/ServerConnections.js
+++ b/src/lib/jellyfin-apiclient/ServerConnections.js
@@ -10,10 +10,16 @@ import { createApiClient } from 'utils/jellyfin-apiclient/createApiClient';
 
 import ConnectionManager from './connectionManager';
 
-const normalizeImageOptions = options => {
+const normalizeImageOptions = (options, apiClient) => {
     if (!options.quality && (options.maxWidth || options.width || options.maxHeight || options.height || options.fillWidth || options.fillHeight)) {
         options.quality = 90;
     }
+
+    // Authenticated image GETs require a query token (browsers cannot send Authorization on <img>).
+    const token = apiClient?.accessToken?.();
+    if (token) {
+        options.ApiKey = token;
+    }
 };
 
 const getMaxBandwidth = () => {
@@ -51,7 +57,17 @@ class ServerConnections extends ConnectionManager {
 
         Events.on(this, 'apiclientcreated', (_e, apiClient) => {
             apiClient.getMaxBandwidth = getMaxBandwidth;
-            apiClient.normalizeImageOptions = normalizeImageOptions;
+            apiClient.normalizeImageOptions = (options) => normalizeImageOptions(options, apiClient);
+
+            // getImageUrl does not call normalizeImageOptions; ensure ApiKey is still attached.
+            const originalGetImageUrl = apiClient.getImageUrl.bind(apiClient);
+            apiClient.getImageUrl = (itemId, options = {}) => {
+                const token = apiClient.accessToken();
+                if (token) {
+                    options.ApiKey = token;
+                }
+                return originalGetImageUrl(itemId, options);
+            };
 
             const api = this.getApi(apiClient.serverId());
 
diff --git a/src/strings/en-us.json b/src/strings/en-us.json
index c18c3ebd40..449f1e3106 100755
--- a/src/strings/en-us.json
+++ b/src/strings/en-us.json
@@ -205,6 +205,7 @@
     "CopyFailed": "Could not copy",
     "CopyStreamURL": "Copy Stream URL",
     "CopyStreamURLSuccess": "URL copied successfully.",
+    "CopyStreamURLError": "Could not create a shareable stream URL.",
     "CopyLogSuccess": "Log contents copied successfully.",
     "CoverArtist": "Cover artist",
     "Create": "Create",
diff --git a/src/utils/dashboard.js b/src/utils/dashboard.js
index b7deb9f8d0..2eb51c066b 100644
--- a/src/utils/dashboard.js
+++ b/src/utils/dashboard.js
@@ -118,7 +118,8 @@ export function getPluginUrl(name) {
 
 export function getConfigurationResourceUrl(name) {
     return ApiClient.getUrl('web/ConfigurationPage', {
-        name: name
+        name: name,
+        ApiKey: ApiClient.accessToken()
     });
 }
 
